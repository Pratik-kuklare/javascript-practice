// logic

const canVote = 20;
if (canVote >= 18) {
    console.log(true);
} else {
    console.log(false);
} // true

const isWeekend = "Sunday";
if (isWeekend === "Saturday" || isWeekend === "Sunday") {
    console.log(true);
} else {
    console.log(false);
} // true

let marks = 50;
let result = marks >= 40 ? "Pass" : "Fail";
console.log(result); // Pass
 