let num = 3;
if(num > 0) {
    console.log(`${num} is a positive number`);
} else {
    console.log(`${num} is a negitive number`);
}

num = -3;
if(num > 0) {
    console.log(`${num} is a positive number`);
} else {
    console.log(`${num} is a negative number`);
}

let isRaining = true;
if(isRaining) {
    console.log('You need to bring a raincoat');
} else {
    console.log('No need to bring a raincoat');
}

isRaining = false;
if(isRaining) {
    console.log('You need to bring a raincoat');
} else {
    console.log('No need to bring a raincoat');
}